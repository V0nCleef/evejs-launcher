"""Read-only Docker Compose runtime support.

This package observes a selected local Compose project.  Read-only log following
is implemented by the dedicated worker module; lifecycle, data, portraits, mods,
and UI features remain outside this runtime package.
"""
